<p id="notice"><%= notice %></p>

<h1>Listing Student</h1>

<table border="2">
  <thead>
    
 
  <tr><td>Enrollment_no</td> <td>Password</td><td>First name</td><td>Middle name</td><td>Last name</td><td>Date of Birth</td><td>Address</td><td>City</td><td>Contactno</td><td>standard</td><td colspan="3"></td></tr> </thead>
    <tbody>
    <% @students.each do |student| %>
      <tr><td><%= student.enrollment_no %></td>
 <td><%= student.password%></td>
  <td><%= student.first_name %></td>
    <td><%= student.middle_name %></td>
    <td><%= student.last_name %></td>
  <td><%= student.date_of_birth %></td>
  <td><%= student.address %></td>
  <td><%= student.city %></td>
  <td><%= student.contactno %></td>
  <td><%= student.standard_id %></td>

<td><%= link_to 'show', student_path(student)%></td>
 <td> <%= link_to 'Edit', edit_student_path(student)%></td>
 <td><%=link_to 'Delete',student_path(student),method: :delete ,data: {confirm: 'Are you sure?'}%></td>

</tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_student_path %>
